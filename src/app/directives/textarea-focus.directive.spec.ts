import { TextareaFocusDirective } from './textarea-focus.directive';

describe('TextareaFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new TextareaFocusDirective();
    expect(directive).toBeTruthy();
  });
});
